DISCORD_TOKEN=cola_o_teu_token_aqui

# Optional — paste your Discord server ID for instant /command updates after deploy
# (right-click server icon → Copy Server ID; enables Developer Mode in Discord settings)
DISCORD_GUILD_ID=0

# Optional — competitive /sudoku challenge + daily achievement claims (memory fallback if unset)
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=sudoku

# --- Discord Activity (pasta activity/) ---
# Application → OAuth2 (Client ID + Client Secret)
# VITE_DISCORD_CLIENT_ID=...
# DISCORD_CLIENT_SECRET=...
